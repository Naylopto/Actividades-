//calcular el perimetro de un circulo
function circuloDiametro(r){
    console.log ( r * 2)
 
}

const circuloDiametro1 = (r) => r * 2; 

//calcular el area de un rectangulo
let pi = 3.1416;
 function circuloArea(r){
    console.log(  (pi * r) * 2 )
 }

 const circuloArea2 = (  (pi * r) * 2 ) => pi * r *2 

//calcular el cuadrado de un numero
function cuadrado(n){
    console.log(n*n)
}
const cuadrado2 = (n * n) => n * n;

//calcular la conversion de grados celsius a farenheit
function gradosCelFa(c){
    return c*1.8+32
}

const gradosCelFa2 = ( c * 1.8 + 32) => c * 1.8 + 32;

//calcular el valor del voltaje dadas la resistencia y la corriente
function volResCor(i,res){
 return i*res
}
const volResCor2 = (i * res) => i * res;

// calcular el volumen de una esfera
function volEsf(rd){
return (4/3)* Math. pi * (rd**3)
}